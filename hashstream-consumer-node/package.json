{
  "name": "hashstream-consumer-node",
  "version": "0.1.0",
  "description": "Sample AWS Lambda consumer for HashStream webhooks, written in TypeScript.",
  "private": true,
  "license": "MIT",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc --noEmit && esbuild src/index.ts --bundle --platform=node --target=node24 --outfile=dist/index.js",
    "lint": "tsc --noEmit",
    "test": "jest"
  },
  "devDependencies": {
    "@types/aws-lambda": "8.10.161",
    "@types/jest": "29.5.14",
    "@types/node": "24.12.4",
    "esbuild": "0.25.12",
    "jest": "29.7.0",
    "ts-jest": "29.4.9",
    "typescript": "5.9.3"
  }
}
